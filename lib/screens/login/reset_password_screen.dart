import 'package:flutter/material.dart';

class ResetPsswordScreen  extends StatelessWidget {
  const ResetPsswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar() ,
      body: Center(
        child: Text('ResetPsswordScreen'),
      ),
    );
  }
}